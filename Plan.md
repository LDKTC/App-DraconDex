-
สร้าง procress.md โดยสร้างตามลำดับการทำงานของplanที่สร้าง เพื่อการแบ่งช่วงการทำงาน
-
-
หลังจากทำเสร็จต้อง x checkbox ด้านล่าง
-
-----
part 1
- [ ] เพิ่มfeature
    - [ ] 1.nexus nest hub -> เพิ่มauto move to เมื่อย้ายmoduleออกจาก/เข้าสู่Parent หรือลากไปวางที่moduleใดmodulesหนึ่ง
    - [ ] 2.context menu nexus nest hub -> เมื่อคลิกที่module ใดสักmodule ให้แสดงเป็นปุ่ม Create ที่เมื่อเอาเมาส์วางให้แสดง module list เป็น submenu list
        - ย้าย module list ที่สร้างได้จากบนcontext menu มาอยู่ในsubmoduleแทน
    - [ ] 3.nav-sidebar button -> เพิ่มปุ่มเพื่อเปิดกลับไปที่nexus nest hub 
part 2
- [ ] แก้feature
    - [ ] 1.scribe -> เมื่อเปิดscribe แล้วกลับมา nexus nest จะมีscribe pane ที่ยังเปิดขึ้นด้านล่างของbuilder pane และปิดไม่ได้ ช่วยทำให้ไม่ขึ้นตอนกลับมาที่่nexus nest hub
    - [ ] 2.hub -> view list ที่ไม่ได้เปิดจะไม่ถูกแทนที่บนhub ให้ใช้การเรียงorder แบบเดิมและใช้การดันorderก่อนหน้าให้ขึ้นไปสุดด้านบนhub แทนการแทนที่ และเมื่อปิดให้ดันลงมาชิดorderด้านล่าง หากเป็นorderล่างสุดอย่างimport dock ให้ดันไปชิดกับnexusheaderที่อยู่ด้านล่างเลย
    - [ ] 3.hub -> view list แต่ละlist ในhub ใช้ containerแยกกัน โดยมีpageของตัวเองที่มี กล่องเก็บcontentของตัวเองแยกกัน และแยกsliderที่ใช้แยกกัน
-----


       