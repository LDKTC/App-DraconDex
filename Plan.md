Plan v2.5.1 remake the UI/UX, use the same theme as and pattern as director module.

    -director module
        -dont make folder button hide from unactive project anymore

    remake UI/UX in all this module, use the same pattern as director module.
        -navigator
        -hero
        -writer
        -sage
    -the submodule should be show on the sidebar as a symbol when active that module project
    

